@model IEnumerable<ManufacturingCompany.Models.Equipment_Maintenance>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}



<p>
    @Html.ActionLink("Create New", "Create", null, htmlAttributes: new { @class = "btn btn-default" })
</p>
<table class="table">
    <tr>
        <th class="text-info">
            @Html.DisplayNameFor(model => model.AspNetUser.UserName)
        </th>
        <th class="text-info">
            @Html.DisplayNameFor(model => model.Equipment.equipment_name)
        </th>
        <th class="text-info">
            @Html.DisplayNameFor(model => model.maintenance_date)
        </th>
        <th class="text-info">
            @Html.DisplayNameFor(model => model.completion_eta)
        </th>
        <th class=" text-right text-info">
            @Html.DisplayNameFor(model => model.maintenance_cost)
        </th>
        <th class="text-info"></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.AspNetUser.UserName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Equipment.equipment_name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.maintenance_date)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.completion_eta)
        </td>
        <td class="text-right">
            @Html.DisplayFor(modelItem => item.maintenance_cost)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id = item.Id }, htmlAttributes: new { @class = "btn btn-warning" })
            @Html.ActionLink("Details", "Details", new { id = item.Id }, htmlAttributes: new { @class = "btn btn-default" })
            @if (User.IsInRole("SuperUser") || User.IsInRole("Manager") || User.IsInRole("Supervisor"))
            {
                @Html.ActionLink("Delete", "Delete", new { id = item.Id }, htmlAttributes: new { @class = "btn btn-danger" })
            }
        </td>
    </tr>
}

</table>
