<div class="container text-center">
    @if (ViewBag.ActionTitle != null)
    {
        if ((string)ViewBag.ActionTitle == "Delete ")
        {
            <h3 class="btn btn-lg text-light bg-danger">@ViewBag.ActionTitle Customer</h3>
        }
        else
        {
            <h3 class="btn btn-lg text-dark bg-success">@ViewBag.ActionTitle Customer</h3>
        }
    }
    else
    {
        <h3 class="btn btn-lg text-dark bg-success">Customers</h3>
    }
</div>

<br />
